- in admin interface, choose source and target panorama within the same tour.
- in admin interface, the first panorama must be within the same tour.
- find a way for scaling hotspot images. a possible way is to add a third optional parameter (distance) after the pan and tild in the photo title.
- find a better way for setting pan & tilt of images. currently in the title of the photo.
- make previous ImageButton disappear when a new one pops up.
